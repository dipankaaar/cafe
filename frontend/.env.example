# Only needed if Backend API is hosted on a different domain than this frontend (e.g. Vercel + Render)
# If deployed together as fullstack, leave this blank or unset.
# VITE_API_URL=https://your-backend.onrender.com/api
